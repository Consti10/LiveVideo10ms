cmake_minimum_required(VERSION 3.6)

#only thing we do here is to include the TelemetryCore.cmake file
#which will build all native libraries for this module

include(${CMAKE_SOURCE_DIR}/TelemetryCore.cmake)
